import Link from 'next/link'; import { Post,Category } from '@prisma/client'; import { ArrowUpRight } from 'lucide-react'; import { formatDate,postText,categoryText } from '@/lib/site';
export function PostCard({post,locale='en'}: {post:Post&{category:Category};locale?:'en'|'te'}){const p=postText(post,locale),c=categoryText(post.category,locale);return <article className="card"><Link href={`/news/${post.slug}`} className="cardimage">{post.imageUrl?<img src={post.imageUrl} alt={post.imageAlt||p.title}/>:<span>No image</span>}</Link><div className="cardbody"><div className="meta"><Link href={`/category/${post.category.slug}`}>{c.name}</Link><span>{formatDate(post.publishedAt)}</span></div><h3><Link href={`/news/${post.slug}`}>{p.title}</Link></h3><p>{p.excerpt}</p><Link className="read" href={`/news/${post.slug}`}>{locale==='te'?'కథనం చదవండి':'Read story'} <ArrowUpRight size={16}/></Link></div></article>}
