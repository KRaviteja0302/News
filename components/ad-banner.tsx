import Link from 'next/link'; import { getActiveAd } from '@/lib/ads'; import { getLocale } from '@/lib/site'; import { advertisementHref } from '@/lib/ad-link';
export async function AdBanner(){const [ad,locale]=await Promise.all([getActiveAd(),getLocale()]);if(!ad)return <div className="publicAdInvite"><div className="shell"><span>ADVERTISEMENT</span><strong>{locale==='te'?'మీ వ్యాపారాన్ని రోజుకు ₹200 నుండి ప్రచారం చేయండి':'Promote your business from ₹200 per day'}</strong><Link href="/advertise">{locale==='te'?'ప్రకటన ఇవ్వండి':'Advertise with us'}</Link></div></div>;return <div className="publicAd"><div className="shell"><span>ADVERTISEMENT</span><Link href={advertisementHref(ad)}><img src={ad.imageUrl||''} alt={locale==='te'&&ad.titleTe?ad.titleTe:ad.title}/></Link></div></div>}
