import { loginAction } from '@/app/actions'; import { HeartPulse } from 'lucide-react';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const error=(await searchParams).error;return <main className="loginPage"><div className="loginCard"><div className="loginLogo"><HeartPulse/>HealthPress CMS</div><h1>Welcome back</h1><p>Sign in to manage every part of your publication.</p>{error&&<div className="error">Incorrect email or password.</div>}<form action={loginAction} className="stack"><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label><button className="primary">Sign in</button></form><a href="/">← Back to website</a></div></main>}
